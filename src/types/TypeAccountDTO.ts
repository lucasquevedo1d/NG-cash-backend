export type postBalanceDTO={
    accountId:string,
    username:string,
    auth:string,
    balance:number
}

export type postBalance ={
    id:string
    auth:string,
    balance:number
}

export type createAccountDTO ={
    id:string,
    balance:number
}